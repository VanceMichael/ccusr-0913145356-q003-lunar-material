# 月面承载边界计算库领域约定

    计算只在已有观测区间内插值，不对未知区间作推断。

    ## 核心对象

    - 材料曲线：统一量纲后的应力沉降点
- 设计申请：基础面积、目标载荷与安全系数
- 计算记录：输入摘要、采用点和边界
- 拒绝原因：数据不足或曲线不满足约束

    ## 业务边界

    - 非单调曲线需要明确拒绝
- 换算精度必须在记录中固定
- 已经形成的计算记录保持不可变

    所有时间字段使用带时区的 ISO 8601 表示；公开标识与数据库自增键分离。接口返回的原因码必须稳定，便于调用方记录和重试。
